# System-wide .profile for sh(1)
# Executed by sh,bash,dash for login shells, should be sourced
# after bashrc

# For Debian,
# see /usr/share/doc/bash/examples/startup-files for examples.
# the files are located in the bash-doc package.


# If not running interactively or already initialized, don't do anything
[ -z "$PS1" ] && return

if [ -f ~/.bashrc ]; then . ~/.bashrc; fi


# Id: user-conf/0.1.0 bash/profile
# vim:ft=bash:
